name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write
  attestations: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable

      - name: Derive release version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Validate release version
        run: |
          grep -q "^version = \"$VERSION\"" crates/cc-codex-proxy/Cargo.toml
          grep -q "^version = \"$VERSION\"" crates/proxy-core/Cargo.toml
          /usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' macos/CCCodexProxy/Info.plist | grep -qx "$VERSION"

      - name: Run Rust tests
        run: cargo test --all

      - name: Build Swift app
        run: swift build --package-path macos/CCCodexProxy

      - name: Build release artifacts
        run: CCP_VERSION="$VERSION" CCP_BUILD_NUMBER="$GITHUB_RUN_NUMBER" CCP_RELEASE_ARCH=arm64 scripts/build-app.sh

      - name: Verify release artifacts
        run: scripts/verify-release-packaging.sh "$VERSION" arm64

      - name: Validate Homebrew metadata
        run: |
          ruby -c Formula/cc-codex-proxy.rb
          ruby -c Casks/cc-codex-proxy-app.rb
          ruby -c packaging/homebrew/cc-codex-proxy.rb
          ruby -c packaging/homebrew/cc-codex-proxy-app.rb

      - name: Attest release artifacts
        uses: actions/attest@v4
        with:
          subject-path: |
            dist/CCCodexProxy-${{ env.VERSION }}-macOS.dmg
            dist/CCCodexProxy-${{ env.VERSION }}-macOS.zip
            dist/CCCodexProxy-macOS.dmg
            dist/CCCodexProxy-macOS.zip
            dist/SHA256SUMS
            dist/RELEASE_MANIFEST.json

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes \
            --verify-tag \
            --draft \
            "dist/CCCodexProxy-$VERSION-macOS.dmg" \
            "dist/CCCodexProxy-$VERSION-macOS.zip" \
            "dist/CCCodexProxy-macOS.dmg" \
            "dist/CCCodexProxy-macOS.zip" \
            "dist/SHA256SUMS" \
            "dist/RELEASE_MANIFEST.json"
          gh release edit "$GITHUB_REF_NAME" --draft=false --latest
